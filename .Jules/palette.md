## 2025-05-22 - [Accessibility in Mobile Navigation]
**Learning:** Icon-only navigation bars on mobile are common but often lack descriptive labels for screen readers, especially when using generic icons like a spark or a calendar.
**Action:** Always add `aria-label` to `Link` and `button` elements in bottom navigation bars when they contain only icons.
